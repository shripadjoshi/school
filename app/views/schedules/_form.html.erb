<%= form_for(@schedule) do |f| %>
  <% if @schedule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@schedule.errors.count, "error") %> prohibited this schedule from being saved:</h2>

      <ul>
      <% @schedule.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :start_date %><br />
    <%= f.datetime_select :start_date %>
  </div>
  <div class="field">
    <%= f.label :end_date %><br />
    <%= f.datetime_select :end_date %>
  </div>
  <div class="field">
    <%= f.label :classroom_id %><br />
    <%#= f.number_field :classroom_id %>
<%= collection_select :schedule, :classroom_id, @classrooms, :id, :name, {:prompt=>true}  %>
  </div>
  <div class="field">
    <%= f.label :teacher_id %><br />
    <%#= f.number_field :teacher_id %>
<%= collection_select :schedule, :teacher_id, @teachers, :id, :name, {:prompt=>true}  %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
